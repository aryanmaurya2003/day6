1 Configure SSH key in github account.

ssh-keygen
cd .ssh/
ls 
cat id_ed25519.pub  
copy this key and paste in github -> setting >add sshkey section





2// Create a repository on github account..
-> Create an account of github 
-> on the nav bar click on the + sign and select the new repository option.
-> Enter the repository name and type description (optional).
-> in the configuration section choose visibility according to your project type .(Public, Private).
-> click on create repository.

3// Clone the above repository on your local from github.
-> git clone git@github.com:aryanmaurya2003/day3.git
-> cd day3/
-> git fetch
-> git branch -a
-> git checkout main
-> ls
->index.html  index.js  style.css

4//
 



